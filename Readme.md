Libraries needed to run these tests can be installed as follows. The Selenium plugin will also need to be installed in Firefox.

    $ pip install selenium
    $ pip install behave

To run the tests:

    $ behave <feature file>
    
